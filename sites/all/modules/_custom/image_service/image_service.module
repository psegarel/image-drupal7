<?php

function image_service_preprocess_block(&$variables)
{
	// 		kpr($variables);
	// if($variables['block_html_id'] == 'block-views-product-block'){
	// 	//kpr($variables);
	// }
}

function image_service_menu_alter(&$items)
{
	// kpr($items);
}